project('structured-project', 'c',
  version : '0.1',
  default_options : ['warning_level=3'])

exe = executable('structured_project', 'src/structured_project.c',
  install : true)

test('basic', exe)
