digraph graph_id {
    // ID=ID;
    // ex) rankdir=LR;

    // attr_stmt
    graph [];
    node [];
    edge [];

    // subgraph
    // ex) subgraph ID {...}

    // node_stmt
    // ex) node_id [...];

    // edge_stmt
    // ex) In digraph => node_id:port -> node_id:port [...];
    // ex) In graph => node_id:port -- subgraph_id:port [...];
    {{#dependencies}}
    "{{from}}" -> "{{to}}";
    {{/dependencies}}
}
